import type { ParamsTypes } from "../../../types/types";
import { Creator } from "../../../utilities/creator";

export class Header extends Creator {
  constructor(objectParams: ParamsTypes) {
    super(objectParams);
  }
}
